<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSS</title>

    <style>
        /* Giving a style to the dropdown button */
        .drpbtn {
            background-color: darksalmon;
            color: black;
            font-size: 20px;
            padding: 4px;
            border: none;
            cursor: pointer;
        }

        /* We are doing position relative here so that dropdown list should occur right below the button. We are also giving position:absolute in .dropdown-content for the same reason*/
        .dropdown {
            position: relative;
            display: inline-block;
        }

        /* We are giving style to the dropdown box */
        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            min-width: 160px;
            box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.2);
            z-index: 1;
        }

        /* Here, we are giving style to the anchor tag which is inside inside the dropdown-content*/
        .dropdown-content a {
            color: black;
            padding: 12px 16px;
            text-decoration: none;
            display: block;
        }

        /* Below, line of code means that inside dropdown-content class there is a <a> tag and if someone hover over the text or box then its background will change */
        .dropdown-content a:hover {
            background-color: #f1f1f1
        }

        /* Below, line of code means that is someone hover over the dropdown then display the dropdown-content*/
        .dropdown:hover .dropdown-content {
            display: block;
        }

        /* Below, line of code means that is someone hover over the dropdown then change the color of the dropbtn button*/
        .dropdown:hover .dropbtn {
            background-color: rgb(247, 135, 97);
        }
    </style>

</head>

<body>

    <h2>Hoverable Dropdown</h2>
    <p>Move the mouse over the text below to open the dropdown content.</p>

    <div class="dropdown">
        <button class="drpbtn">Click Here</button>
        <div class="dropdown-content">
            <a href="#google">Google</a>
            <a href="#youtube">Youtube</a>
            <a href="#instagram">Instagram</a>
        </div>
    </div>

</body>

</html>